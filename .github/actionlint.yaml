# SPDX-FileCopyrightText: Vernum Projecten B.V.
# SPDX-License-Identifier: Apache-2.0
#
# actionlint configuration.
# https://github.com/rhysd/actionlint/blob/main/docs/config.md

# The organisation's runners (Vernum-Projecten/hetzner-runners) carry the
# standard labels self-hosted, linux and x64, which actionlint knows, plus
# this one custom label.
self-hosted-runner:
  labels: [hetzner]

# null disables the configuration-variables check, so a `vars.*` added later
# is accepted without being listed here.
config-variables: null
